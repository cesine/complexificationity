#!/usr/bin/env node
var service = require('../service');

service.set('port', process.env.PORT || 3000);

var server = service.listen(service.get('port'), function() {
  console.log('Express server listening on port ' + server.address().port);
});
